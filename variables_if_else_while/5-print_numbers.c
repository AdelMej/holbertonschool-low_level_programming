#include <stdio.h>

/**
* main - prints all single digit numbers of base 10
*
* Return: always zero
*/

int main(void)
{
int x = 0;
while (x < 10)
{
printf("%d", x);
x++;
}
printf("\n");
return (0);
}
