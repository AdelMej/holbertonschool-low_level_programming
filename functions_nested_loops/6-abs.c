#include "main.h"
#include <stdlib.h>

/**
* _abs - a function that returns an absolute value of a given number
* @n: an input int that will be returned as absolute
*
* Return: the basulte value of n
*/

int _abs(int n)
{
	return (abs(n));
}
